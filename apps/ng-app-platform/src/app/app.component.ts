import { Component } from '@angular/core';

@Component({
  selector: 'ng-app-platform-root',
  template: `<router-outlet></router-outlet>`,
})
export class AppComponent {
  title = 'ng-app-platform';
}
